import {IonicHeaderTitle} from './IonicHeaderTitle';

export class IonicHeader {
  identity: string;
  title: IonicHeaderTitle;
  backButton: string;
  basket: boolean;
  exit: boolean;
  search: boolean;
  route: string;
}
